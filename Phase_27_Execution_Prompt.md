# Phase 27 Agent Execution Prompt

**Goal:** Ruthlessly eliminate false positive trading signals to ensure the live dashboard only displays highly accurate, high-probability A+ setups. Do not build new UI features or backtesting tools.

You are picking up from Phase 26. The live trading dashboard is fully built, but the user needs the signals to be **highly accurate** before making real trades. Your sole objective is to eliminate false positives in the signal generation logic inside `engine.py` and `intelligence/`. 

**Execute these three precise logic upgrades in the Python backend:**

1. **Multi-Timeframe Confluence (The Absolute Gate):** 
Modify the `compute_score` logic so that a 5m or 15m `LONG` signal is completely killed (downgraded to WATCH or WAIT) if the 1-hour or 4-hour trend is overwhelmingly bearish. A short-term bounce against a strong macro downtrend should never trigger an A+ EXECUTE.

2. **Order Flow Veto (Delta Alignment):** 
Add a strict check for the existing `flows` data. If a `LONG` is generated by technicals, but the short-term volume delta is heavily negative (aggressive market selling), instantly cancel or downgrade the signal. Price action must be supported by actual order book flow.

3. **Volatility & Range Rejection (Chop Filter):** 
If the market is in a chop/ranging state (low volume, flat ATR) and price is in the middle of the Bollinger Bands/VWAP, reject all signals. Only fire when price is at the *extremes* of the range (mean reversion) or in clear expansion (breakout).

**Rules:**
- Do not write new HTML/JS or complex UI. 
- Only harden the existing Python logic in `engine.py` (and relevant `intelligence/` modules).
- The dashboard should only light up `A+ EXECUTE` when the odds are overwhelmingly mathematically stacked in the user's favor.
